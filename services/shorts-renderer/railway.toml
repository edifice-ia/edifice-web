[build]
builder = "DOCKERFILE"
dockerfilePath = "services/shorts-renderer/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
